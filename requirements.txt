"""
ARM64-friendly requirements for the fusion service.

Removed build-heavy optional dependencies (e.g. pyarrow, httptools) to
avoid MSVC/CMake build failures on Windows ARM64 targets. Use
`requirements-arm64.txt` at the repo root for a combined, ARM64-safe
set including dashboard deps when running on ARM64 Windows.
"""

fastapi>=0.111.0
uvicorn>=0.30.1
websockets>=12.0
requests>=2.32.3
python-dotenv>=1.0.1
pyttsx3>=2.90

# Optional monitoring
psutil>=5.9.0
